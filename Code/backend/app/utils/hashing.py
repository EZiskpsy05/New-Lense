#Jobs
    #stable IDs and privacy friendly storage
        #deduping : detect if user submists same article again
        #caching : reuse previous analysis result quickly  
#importance 
    #don't want to store huge text
    #text may contain private data 
    #hash is fixed size & safer to store index 