#1. Jobs
    #credibility computed from scoring function --> explanation by ai.api
    #compute credibility score from weighted mix of signals
        #domain reputation (basic allowlist / known outlets)
        #presence of author / date / citations
        #sensational language indicators (use data from the bias / disinfo signals)
        #cross reference agreement (check if multiple source report the same core claims)
        #satire likelihood (reduces credibility score differently <-- need to choose this)
        #misinfo / disinfo
    #LLM assisted credibility rationale
        #use an ai.api to write explanation based on the scoring function
        #need to set a format to produce the same format every time
    #Returns actionable guidance(ai.api) + missing trust signals