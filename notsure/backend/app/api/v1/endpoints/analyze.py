#1. POST /api/v1/analyze accepts text / url / pdf upload
#2. Calls the analysis pipeline
#3. Returns the 6 outputs in a frontend-friendly “tabs” response (scores + evidence highlights + explanations + recommendations)